# Handoff — where this work stands, 2026-09-17

Written to be read first by whoever picks this up next, in place of re-reading the session. The
long documents it points at are the evidence; this page is only the state.

---

## 1. What is being built, in one paragraph

`vc-knowledge` becomes the single knowledge base for Virto Commerce, and `vc-kb` becomes a plugin
that reads it. The 1.2 MB of hand-written knowledge in `vc-mcp-testing-module/plugins/vc-fix/knowledge/`
is **leaving the plugin and the project** and migrates into the base in the base's own form. The base
is obtained from `https://github.com/VirtoCommerce/vc-knowledge.git` and is never carried inside a
plugin. The goal for the demo is decision §3.1 of the review: show that the accumulation loop works,
not that anything got faster.

## 2. The documents

| file | what it is |
|---|---|
| `measurements/REVIEW-PLUGIN-PATH-2026-09-16.md` | **the review**. Verdict per goal, 11 ranked defects, the integration decision, the architecture recommendation against published practice, the plan, the demo script, open questions. Untracked |
| `measurements/REVIEW-PLUGIN-PATH-2026-09-16-overlap.md` | all 88 active entries + 3 flows classified against the 34 `vc-fix/knowledge` files, twice; plus every 4th `BL-*` invariant the other way. Untracked |
| `measurements/REVIEW-PLUGIN-PATH-2026-09-16-sources.md` | 31 external sources fetched, with dates, quotes and relevance. Untracked |
| `measurements/IMPORT-BUSINESS-LOGIC-2026-09-17.md` | the importer's dry run: what each of the 216 rules becomes, the contradictions, the modules the page cites, and what a reader will and will not reach |
| `measurements/IMPORT-BUSINESS-LOGIC-2026-09-17-sample.md` | 20 imported entries, rendered, for a person to read before the live write |
| this file | the state |

Everything older — `REVIEW-ARCHITECTURE-RESPONSE.md`, the errata, the round pages — is unchanged and
still the record.

## 3. The owner's decisions, all of them

Taken 2026-09-16 and 2026-09-17. Treat as given; reopen only with evidence.

1. `vc-fix/knowledge` leaves the plugin and the project; it migrates into `vc-knowledge`.
2. The base comes from the Virto repository, never from inside the plugin.
3. The plugin's own operating instructions (`execution/*`, `diagnostics/*`, `shared-instructions`,
   `browser-quirks`, `storefront-selectors`, `architecture/*`, `graphql-test-cases-runner`) **stay
   with the skills**. They are not knowledge about the platform.
4. **Migration first, then the demo** — stream B before stream A.
5. The base is connected by **`/project-init`**: find an existing checkout (`KB_BASE` or a sibling
   `vc-knowledge`), else `git clone --depth 1` into `<project>/.vc-knowledge`; record
   `knowledgeBase: { repo, path, ref }` in `project-profile.json`; readiness row in `verify-access`;
   `--check` adds the field to old profiles. The vc-kb SessionStart hook only pulls.
6. Virto's agents **push to `main`**; CI runs `kb validate` and rebuilds the generated indexes and
   catalogs. Pull requests only for migrations.
7. ECL's **generic chapters are not touched**. Its 182 `[OBSERVED]` rows and chapter 14 migrate.
8. **BL ids stay in the entry subject**, with `kb show BL-…` resolving by that id.
9. **The plugin copy is canonical** after the re-port; the lab keeps `measurements/` and the
   archives and imports the tool from the plugin.
10. Contract copies (`api/graphql-schema.md` and the pasted field tables) are **deleted**, not
    migrated: the derived plane is their right form and `kb check` byte-compares it.
11. **Nothing is imported as `confirmed`.**
12. The demo runs **without the live arrival hook**; the hook is cited as a replayed measurement.
13. The rehearsal is a rehearsal, not a sixth measured round; its numbers stay labelled.

## 4. What is done

**B1 — the `normative` plane exists.** Rules are a third written store, beside `captured/` and
`flows/`.

| | |
|---|---|
| new | `src/rules.mjs` (id grammar, domain key, catalog order), `test/rules.test.mjs` (13 tests) |
| changed | `src/planes.mjs` (`WRITTEN_STORES.normative` → `rules/`), `src/capture.mjs` (identity by rule id, `NORMATIVE_EXEMPT`, the unattested first row, the empty-store guard, `attestedOf` tightened), `src/coordinates.mjs` (rules in the coordinate index, `writtenNeighbours`), `src/validate.mjs` (the rules store's gate), `bin/kb.mjs` (`--rule`, `kb rules`, `kb show BL-…`, stat, reindex, usage) |
| verified | **285 tests pass** (269 before; B2 added three more). The live corpus rebuilds **byte-identically**; `kb validate` on a clone is green — 590 derived, 100 captured, 3 flows, 0 rules, 39 notices, unchanged |

Three design decisions inside it, each argued at the point of code:

* **A transcribed rule does not vote.** Its first row is kept whole and marked `attested: false`
  with a `whyNot`, so a rule read off a page reads 0 parties. No import arrives one confirmation
  short of the licence to act unverified.
* **A rule answers no question and needs no scope.** Both exempt at the door and the gate; requiring
  either would make an importer invent 216 unobserved values.
* **An observation does not supersede a rule.** The door points at `kb dispute`; both sides stay.

One latent defect fixed on the way: `kb reindex` wrote an index and a catalog for an empty store,
and `kb validate` then failed on that verb's own output. Latent in the flow store since it shipped.

**Housekeeping done:** the 14 probe rows a previous review pass left in the live `demand.jsonl` are
discarded; the diff is in the session scratchpad. `vc-knowledge` is clean.

**B2 — the importer is written and verified against a copy.** `scripts/import-business-logic-2026-09-17.mjs`,
dry run by default, writes through `capture()`.

| | |
|---|---|
| verified | a trial `--write` into a copy of the base: **216 captured, 0 refused at the door**, `kb validate` **OK**, 7 notices from the new plane against 57 for the corpus. All 216 bodies **byte-identical** to their block in the source page — 375,415 characters, nothing lost. `kb rules`, `kb rules BL-CART`, `kb show BL-CART-003` all read correctly, and every rule reads **0 parties** |
| tool changes it needed | `anchors` joins `question` and `appliesTo` in `NORMATIVE_EXEMPT`, and the gate stops reporting a rule with none; a relative `--from` now resolves against the **base** rather than the CLI's working directory, so the path stored in a row travels with the corpus. Three tests |
| not run against `vc-knowledge` | the live base is untouched. The handoff's own rule — 20 read by a person first — has not been met yet |

Three things the plan expected that the document did not support, each measured rather than assumed:

* **143 of 216 rules name no coordinate**, and only 49 name one this base resolves. They are not
  badly written: "money rounds half-up to two decimals" is about the platform, not a place in it.
  A rule is reached by its id and its domain. So this is counted, not queued, and the door no
  longer asks a rule for an anchor.
* **`--source` is not used at all.** Passing it would mark the row as code somebody read, and
  nobody in this import read any C#; the page did. The `Source:` line stays in the body verbatim.
  Which modules the base does not record as installed — `vc-module-loyalty`, `vc-module-sales-rep`,
  `vc-module-x-pickup`, 21 rules between them — is reported instead.
* **Nothing collides.** On this plane identity is the rule id, so a fingerprint collision could only
  mean "already imported". The 13 contradictions the annex found do not collide with anything: a
  rule and an observation are different planes. Ten of the 13 involve this page and are seeded by
  hand from the annex's own rows, because two of them name no coordinate and a regex cannot see
  either. They are imported, flagged, and the report names the `kb dispute` that settles each.

**What is held back:** nothing, as it turns out. Every one of the 216 passes the door.

## 5. What is next, in order

| # | step | est. |
|---|---|---|
| **B2b** | **read the 20** in `IMPORT-BUSINESS-LOGIC-2026-09-17-sample.md`, then `node scripts/import-business-logic-2026-09-17.mjs --write` against `C:/_VIRTO/vc-knowledge`, `kb validate`, commit the base | 1 h |
| **B2c** | the ten contradictions: read each pair, then either `kb dispute <KB id>` or — for the six B2B ones the annex reads as a module-mix difference — a scope axis on the rule | 2 h |
| A1 | re-port the tool to `plugins/vc-kb`, make the plugin canonical, lab imports it by one path constant | 2.5 h |
| A2 | `.claude-plugin/plugin.json` + marketplace entry | 1.5 h |
| A3 | base via `/project-init` (decision 5) | 3 h |
| A4 | SessionStart hook: written catalog + the 24-line rules domain index | 3 h |
| A6 | the skill (`skills/register/SKILL.md`, under 500 lines) | 3 h |
| A9 | fresh `kb extract`, then `refute`, then `refute --baseline` — one authorized session | 2 h |
| B3 | import `vc-bug-catalog.md` (55) and ECL `[OBSERVED]` (182) + chapter 14 | 3 h |
| B4 | delete the contract copies, re-point the agents at `kb rules` / `kb show BL-…` | 2.5 h |
| later | A5 arrival hook with filters, A11 rehearsal — outside the 30 h |

## 6. Open questions

1. **Can `kb ask` reach a rule?** Today it cannot: `resolve.mjs` loads the derived, captured and
   flow indexes and not the rules index, so "does a coupon apply to the sale price" returns the
   observations around `BL-CART-003` and never the rule itself. Arguably the design — a rule is
   found by working in its domain, which is why the catalog has no question column. The shape that
   would not repeat the dead-weight failure is rules in the ranked list but in their own labelled
   block, after what somebody actually saw. Belongs with A4 and A6.
2. **Tier two** — 13 request-reachable licensed entries, one authorized session against the stand,
   rides on A9. Before or after the demo.
3. **Push rights in practice** — decision 6 says push to `main`; who sets up the CI that rebuilds
   the indexes, and in which repository does that workflow live.

## 7. Hard rules, still true

* `kb ask`, `kb how` and `kb deliver` **write** to the demand loop. Probe against a copy with
  `--base`, and pass a drive-letter path.
* Never run anything against the deployment. Read-only scripts and archived logs only.
* Writing verbs fail silently on a base that is not a base — `capture` refuses without `kb.json` now,
  and that refusal is the reason.
* Working inside `vc-kb-lab`, every tool call runs the lab's own `PostToolUse` hooks: text that
  merely mentions a route injects register entries and writes to `MEASUREMENT/`. Archive
  `MEASUREMENT/` before any measured run.
* Pin the source, not the value. A number copied onto a page drifts from the script that made it.
